# Bootstrap changes

## Visit the Store - Arsen Ibatullin

- Replaced the hand-built navigation list with Bootstrap `navbar`, `navbar-expand-lg`, `navbar-nav`, `nav-item`, and `nav-link` classes.
- Replaced manual main width and padding on `visit.html` with Bootstrap `container` and `py-5`.
- Replaced manual section spacing and layout with `row`, responsive `col-12`, `col-md-*`, `col-lg-*`, `g-4`, `mb-5`, and `pt-4`.
- Replaced the old visit-page image sizing, border, and border-radius rules with Bootstrap `img-fluid`, `border`, and `rounded`.
- Replaced manual link-button styling with Bootstrap `btn`, `btn-outline-secondary`, and `btn-sm`.
- Added a Bootstrap `alert` component for the visit-planning notice.

## Care and Storage - Arsen Ibatullin

- Replaced the hand-built navigation list with Bootstrap `navbar`, `navbar-expand-lg`, `navbar-nav`, `nav-item`, and `nav-link` classes.
- Replaced manual main width, section spacing, and topic-link layout with Bootstrap `container`, `nav`, responsive display classes, and spacing utilities.
- Replaced the custom definition-list grid with a Bootstrap `row` and responsive `col-sm-*` classes.
- Replaced custom table sizing, borders, padding, and row backgrounds with Bootstrap `table`, `table-bordered`, `table-striped`, `align-middle`, and `table-responsive`.

## About the Project - Arsen Ibatullin

- Replaced the old separate navigation list with Bootstrap `navbar`, `navbar-expand-lg`, `navbar-nav`, `nav-item`, and `nav-link` classes.
- Replaced manual main width, paragraph spacing, and section borders with Bootstrap `container`, `py-5`, `border-top`, `pt-4`, and margin utilities.
- Replaced custom code-block and validation-message layout with Bootstrap `p-3`, `border`, `rounded`, `bg-light`, `overflow-auto`, and display utilities.
